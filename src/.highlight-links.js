const style = document.createElement('style'); const styles = ':root .highlight-link { background-color: yellow !important }'; if (typeof style['styleSheet'] === 'object') { style['styleSheet'].cssText = styles; } else { style.appendChild(document.createTextNode(styles)); } document.head.appendChild(style); Array.from(document.links).forEach((link) => { link.classList.add('highlight-link'); })